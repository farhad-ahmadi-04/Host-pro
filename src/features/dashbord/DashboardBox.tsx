import { Card, CardHeader, CardTitle } from "@/components/ui/card";

function DashboardBox() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Dashboard box title</CardTitle>
            </CardHeader>
        </Card>
    );
}

export default DashboardBox;